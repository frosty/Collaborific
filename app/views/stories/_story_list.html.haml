%ul{:id => "stories_list"}
  -for story in @stories
    %li
      %strong= story.title
      %p
        Creator: 
        = story.owner.login if story.owner
      %p
        Description: 
        = story.description
  -if @stories.empty?
    %li No stories added yet...